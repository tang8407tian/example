<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ttac.service.mybatis.dao.mapper.UserMapper">


    <update id="updateBalance">
        UPDATE `user` AS u SET u.balance = #{po.balance}  , u.version =#{po.version}  WHERE u.id = #{po.id}


    </update>
</mapper>
